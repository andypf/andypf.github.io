{
  "name": "pfau-software",
  "type": "module",
  "scripts": {
    "dev": "serve -l tcp://0.0.0.0:$APP_PORT src",
    "prod": "serve -l tcp://0.0.0.0:$APP_PORT dist",
    "build": "node build.js",
    "clear": "rm -rf dist",
    "deploy": "git subtree push --prefix dist github-pages gh-pages"
  },
  "dependencies": {
    "serve": "^13.0.2"
  },
  "devDependencies": {
    "fs-extra": "^10.1.0",
    "glob": "^8.0.3",
    "minify": "^9.1.0"
  },
  "version": "0.0.0"
}
